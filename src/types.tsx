export type NumberData = { title: string, number: string }

export type Feature = { title: string, description: string, icon: string, alt: string }

export type Quote = { quote: string, avatar: string, alt: string, name: string, position: string }